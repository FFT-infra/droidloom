<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      The development supervisor creates the matching 10.177.0.1/30 gateway.
      Public resolvers keep the cell independent from the host's resolver
      implementation while packets still use the host's existing uplink.
    -->
    <string-array translatable="false" name="config_ethernet_interfaces">
        <item>eth0;*;ip=10.177.0.2/30 gateway=10.177.0.1 dns=1.1.1.1,8.8.8.8</item>
    </string-array>
</resources>
