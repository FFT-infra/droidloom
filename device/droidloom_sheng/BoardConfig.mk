# Android's board lookup scans vendor/*/<device> with a bounded depth. Keep the
authoritative product boundary grouped under android/device and expose it
here when this repository is projected at vendor/droidloom.
include vendor/droidloom/android/device/droidloom_sheng/BoardConfig.mk
